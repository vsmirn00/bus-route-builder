#import streamlit as st
#from app.context_manager.app_lifespan import interactive_maps, requests
#from streamlit_folium import st_folium

# Backend API endpoint
#API_URL = "http://localhost:8000/map"

#st.title("Map Viewer")

# Fetch the map from the FastAPI backend
#request = requests[requests.name]
#if interactive_maps[request.name] is not None:
    # Display the Folium map interactively
 #   st_folium(interactive_maps[request.name], width=725, height=500)
#else:
#    st.error("No map available. Generate a map first.")
